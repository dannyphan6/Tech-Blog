<div class="card mb-4 mt-4 ml-4 mr-4">{{#each posts as |post|}} 
    <div class="card-header">
        <p class="custom-p">{{post.title}}</p>
    </div>
    <div class="card-body">
        <p class="custom-p">{{post.body}}</p>
    </div>
    {{/each}}
</div>