{{#each posts as |post|}} 
<div class="card m-3">
    <div class="card-header">
        <p class="custom-p">{{post.title}}</p>
    </div>
    <div class="card-body">
        <p class="custom-p">{{post.body}}</p>
    </div>
</div>
{{/each}}